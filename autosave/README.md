# Autosave information

This directory will be populated with the save/restore files to retain settings between IOC reboots.

The distribution directory does not contain an auto_settings.sav file, but this will
be created the first time the IOC is run.
